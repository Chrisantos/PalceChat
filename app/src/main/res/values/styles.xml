<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppBarBottom" parent="Theme.AppCompat.Light.DarkActionBar" >
        <item name="colorAccent">@color/white</item>
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
    </style>

    <style name="success">
        <item name="textBold">false</item>
        <item name="textColor">#fff</item>
        <item name="textSize">14sp</item>
        <item name="colorBackground">#00C853</item>
        <item name="solidBackground">true</item>
        <!--<item name="strokeWidth">3dp</item>-->
        <!--<item name="strokeColor">#fff</item>-->
        <item name="iconLeft">@android:drawable/ic_popup_sync</item>
        <item name="length">LONG</item>
        <item name="cornerRadius">18dp</item>
    </style>

    <style name="error">
        <item name="textBold">true</item>
        <item name="textColor">#fff</item>
        <item name="textSize">14sp</item>
        <item name="colorBackground">#F44336</item>
        <item name="solidBackground">true</item>
        <!--<item name="strokeWidth">3dp</item>-->
        <!--<item name="strokeColor">#fff</item>-->
        <item name="iconLeft">@android:drawable/ic_dialog_alert</item>
        <item name="length">LONG</item>
        <item name="cornerRadius">18dp</item>
    </style>

</resources>
